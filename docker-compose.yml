services:
  autonomous-bot:
    build: .
    image: fantasy-bot:latest
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - .env
    command: ["python", "-m", "prediction.docker_langchain_autonomous"]
    restart: unless-stopped
